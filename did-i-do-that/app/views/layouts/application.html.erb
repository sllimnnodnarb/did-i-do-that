<!DOCTYPE html>
<html>
  <head>
    <title>DidIDoThat</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Josefin+Sans|Shadows+Into+Light" rel="stylesheet">
  </head>
  <body>
    <div class="container">
      <ul class="nav nav-tabs">
        <div>
          <% if user_signed_in? %>
          <%= image_tag current_user.avatar_url(42), class: "gvtr" %>
          <% end %>
          <small class="pull-right">
            <% if user_signed_in? %>
              <small class="pull-right">Signed in as: <strong><%= link_to current_user.email, users_show_path %></strong></small><br>
              <small><%= link_to "Edit profile", edit_user_registration_path, class: 'pull-right bbfont' %>
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "pull-right bbfont" %><br>
            <% else %>
              <%= link_to "Sign Up", new_user_registration_path, class: "pull-right bfont"  %>
            <% end %>
            </small>
          </small>
        </div>
      </ul>
      <% if flash[:notice] %>
        <div class="alert alert-success cc">
          <button type="button" class="close" data-dismiss="alert">&times;</button>
          <%= flash[:notice] %>
        </div>
      <% elsif flash[:alert] %>
        <div class="alert alert-warning cc">
          <button type="button" class="close" data-dismiss="alert">&times;</button>
          <%= flash[:alert] %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
